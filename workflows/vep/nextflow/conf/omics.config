params {
    vep_container = ''
    outdir = '/mnt/workflow/pubdir'
}

manifest {
    nextflowVersion = '!>=22.04.0'
}

conda {
    enabled = false
}

process {
withName: '.*' { conda = null }
withName: '(.+:)?ENSEMBLVEP' { container = "${ params.vep_container }" }
}